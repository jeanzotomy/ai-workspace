# Secrets & env (jamais commités) — couvrir TOUS les backups, pas juste .env exact
.env
.env.local
*.env.local
.env.*
!.env.example
*.bak
*.bak-*
config/authentik/.secrets/
scripts/.generated/

# Clés & certificats TLS (auto-signés locaux et autres)
*.key
*.crt
proxy/certs/

# Volumes & données runtime
volumes/
data/
*.pem
acme.json

# Logs
*.log
logs/

# Vendor (Comp AI — cloné par bootstrap.sh, ne pas commiter)
vendor/

# Build artifacts Comp AI
compose/comp-ai-build/

# Traefik ACME (stocké dans volume Docker — ne pas commiter)
proxy/acme.json

# OS / éditeurs
.DS_Store
Thumbs.db
.idea/
.vscode/

# Handoffs internes agents (workflow)
_*-output.md
